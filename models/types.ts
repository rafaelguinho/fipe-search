export interface BrandsApi {
  nome: string;
  codigo: string;
}

export interface ModelsApi {
  nome: string
  codigo: number
}

export interface SelectOption {
    label: string;
    value: string;
  }
